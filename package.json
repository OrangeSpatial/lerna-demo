{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "version": "lerna version",
    "test": "lerna run test"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  },
  "version": "0.0.1"
}
